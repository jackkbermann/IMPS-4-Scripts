from pco import Camera
from constants import EXPOSURE_TIMES
import numpy as np 
import matplotlib.pyplot as plt


# Constants

for name, time in EXPOSURE_TIMES.items():
        print(f"  {name}: {time} ms")
